import React from "react";
import "./../styles/Header.css";

const Header = () => (
  <header className="header">
    <h1>Select Playlist</h1>
    <p>Discover playlists curated for every mood and moment.</p>
  </header>
);

export default Header;
